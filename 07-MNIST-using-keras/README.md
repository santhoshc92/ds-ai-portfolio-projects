**Overview**

This project implements a handwritten digit classification system using the MNIST dataset and Keras. The model is a deep learning neural network trained to recognize digits (0–9) from grayscale images.
Evaluation

**Common evaluation metrics for MNIST digit classification:**

Accuracy: Percentage of correctly classified digits.

Confusion Matrix: Visualizes misclassifications between digits.

Loss Curves: Training and validation loss over epochs.

**Dependencies:**

TensorFlow / Keras

NumPy

Matplotlib